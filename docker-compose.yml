version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: nginx-api-proxy
    ports:
      - "8888:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

networks:
  backend-network:
    driver: bridge